import {useEffect,useRef} from "react";
export default function WatermarkOverlay({user}){const ref=useRef(null);useEffect(()=>{const canvas=ref.current,ctx=canvas.getContext("2d");const draw=()=>{canvas.width=canvas.clientWidth;canvas.height=canvas.clientHeight;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.font="16px sans-serif";ctx.fillStyle="#111";ctx.rotate(-Math.PI/8);const label=`${user?.name??"사용자"} ${user?.employeeNo??""} · ${new Date().toLocaleString()}`;for(let y=-canvas.height;y<canvas.height*2;y+=100)for(let x=-canvas.width;x<canvas.width*2;x+=360)ctx.fillText(label,x,y)};draw();const timer=setInterval(draw,60000);window.addEventListener("resize",draw);return()=>{clearInterval(timer);window.removeEventListener("resize",draw)}},[user]);return <canvas ref={ref} aria-hidden="true" style={{position:"absolute",inset:0,width:"100%",height:"100%",pointerEvents:"none",opacity:.15,zIndex:10}}/>}
