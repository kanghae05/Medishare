import {useEffect,useState} from "react"; import {useNavigate,useParams} from "react-router-dom"; import {getNotice,saveNotice} from "./noticeApi";
export default function NoticeForm(){const {noticeId}=useParams();const nav=useNavigate();const [form,setForm]=useState({title:"",content:"",pinned:false});useEffect(()=>{if(noticeId)getNotice(noticeId).then(setForm)},[noticeId]);
 const change=e=>setForm(f=>({...f,[e.target.name]:e.target.type==="checkbox"?e.target.checked:e.target.value}));return <form onSubmit={async e=>{e.preventDefault();const saved=await saveNotice(noticeId,form);nav(`/notices/${saved.noticeId}`)}}><h2>공지사항 {noticeId?"수정":"작성"}</h2><input required className="form-control mb-3" name="title" value={form.title} onChange={change} placeholder="제목"/><textarea required className="form-control mb-3" rows="14" name="content" value={form.content} onChange={change} placeholder="내용 (WYSIWYG 에디터 어댑터 연결 지점)"/><label className="form-check mb-3"><input className="form-check-input" type="checkbox" name="pinned" checked={form.pinned} onChange={change}/><span className="form-check-label">상단 고정</span></label><button className="btn btn-primary">저장</button></form>}
